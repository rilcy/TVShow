<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="devmobile.tvshow" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ByShow" >
        </activity>
        <activity android:name=".BySeason" >
        </activity>
        <activity android:name=".ByEpisode" >
        </activity>
        <activity android:name=".ByShowEdition" >
        </activity>
        <activity
            android:name=".ByShow_Edition"
            android:label="@string/title_activity_by_show__edition"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
    </application>

</manifest>
